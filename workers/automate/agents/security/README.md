# Security

Entradas operacionais do agent `Security` dentro de `workers/automate/agents/`.

## Arquivos

- `dispatch.mjs`: caminho preferencial para o runner operacional do agent em `Work`
- `review.mjs`: entrada de compatibilidade para a base executável de revisão de Security

## Observação

A política de Security continua em `workers/automate/security-review.md` e `workers/automate/security-project-status.md`, enquanto a entrada oficial do agent passa a ser exposta por esta pasta.
