# Agent wrappers

This directory is reserved for thin agent wrappers. The canonical role and
skill contracts live under `agents/roles` and `agents/skills`.

The Copilot dispatch surface remains paused; adding this marker does not
enable automatic dispatch.
